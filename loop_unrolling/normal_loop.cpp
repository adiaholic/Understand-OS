#include <iostream>

int main() {
    const int N = 1000;  // Define array size
    int a[N];            // Declare array

    // Your original loop
    for (int i = 0; i < N; i++) {
        a[i] += 1;
    }

    return 0;
}
